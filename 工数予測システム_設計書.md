# 工数予測システム 設計書

過去プロジェクトの実績工数から、未来プロジェクトの月次工数を予測する。
本書は実装着手前の設計合意をまとめたもの。決定事項には、後から判断を再検討できるよう
**意図・理由**を併記する。

---

## 1. 目的とスコープ

### 目的
契約人月・期間が決まっている未来プロジェクトについて、
**月 × 行程グループ の工数配分**を予測する。

### 前提
- 決まった人月は使い切る。よって予測すべきは工数の「量」ではなく **「形」(配分)**
- 過去実績は月次で記録されている
- 実績データは時間単位、契約は人月単位

### 初期スコープ(第一版で作るもの)
- 月 × 行程グループ の工数予測表
- 学習カーブの可視化
- leave-one-out による精度検証

### 初期スコープ外(構造は用意するが出力しない)
| 項目 | 理由 |
|---|---|
| 所属・チーム・メンバー別の内訳 | 主軸の予測が出た後で足せる。データには保持する |
| 頭数・稼働率の推定 | 同上 |
| ピーク要員の警告、調達可能性の検証 | 同上 |
| 行程グループより細かい行程単位の出力 | 統計的に弱く、予測の責任を負わせない |
| HTML / Web UI | 探索段階ではExcelが有利。共有段階で検討 |

---

## 2. 用語定義

議論の途中で「チーム」が二重の意味で使われたため、明確に区別する。

| 用語 | 定義 | 役割 |
|---|---|---|
| **行程** | 実績に記録されている作業区分。20〜30種類と細かい | 実績の記録単位 |
| **行程グループ** | 行程を5〜7個に束ねたもの。**見積もりの分類と一致する** | **予測の主軸** |
| **所属** | 会社・部署単位(自社、協力A 等) | 補助情報 |
| **チーム** | 所属の中の小分類 | 補助情報 |
| **種別** | プロジェクトの性質(製品本体開発 / 大規模アップデート 等)。必須・排他 | 第一分類 |
| **タグ** | 種別の中での特性(新規/改修、開発手法 等)。複数付与可 | 微調整 |
| **マイルストーン** | α版、β版など。案件あたり2〜7個 | 時間軸のアンカー |

---

## 3. 基本方針

### 3-1. 案A ─ 消化率は動かさず、時間軸だけを伸縮する

マイルストーンで指定された期間が過去実績の形と食い違う場合、
**工数比率は学習値のまま維持し、期間だけを伸ばす**。

- 例:過去実績で要件定義が総工数の12%なら、指定期間が長くても短くても12%
- 帰結:期間が長い区間は要員密度が薄くなる

**意図** ─ 「期間が延びるのは承認待ちや会議間隔が理由であり、作業量が増えるわけではない」
という実態に合わせた判断。工数を期間に連動させる案B・案C、および両者を弾力性パラメータで
連続的に扱う案は検討のうえ却下した。

**副作用として認識しておくこと** ─ 前半の期間が長いと後半に工数が集中する。
将来ピーク要員の警告機能を追加する際は、この性質が理由になる。

### 3-2. 制約は「必須」ではなく「あれば効く」

マイルストーン情報、見積もり分類、種別、タグ ─ いずれも**無くても動く**設計とする。
情報が増えるほど滑らかに精度が上がる構造にする。

**意図** ─ 過去データの整備状況が案件ごとにバラつくため。
「マイルストーンがある前提」で作ると、記録の無い案件が学習から丸ごと脱落する。

### 3-3. 主軸と属性を分離する

- **主軸**:月 × 行程グループ → 工数(総量に責任を持つ)
- **属性**:各セル内の所属・チーム構成比(参考情報)

**意図** ─ 属性が欠損しても主軸の予測が壊れないようにする。
所属が空欄の実績行も、行程と時間さえあれば主軸の学習に貢献できる。

### 3-4. 学習は行程グループ単位で行う

20〜30の行程で直接学習しない。

**意図** ─ 10案件 × 15ヶ月では、細分化された行程1つあたりの観測点が少なすぎる。
加えて細かい行程は「案件によって存在しない」「名前が違う」「まとめて計上されている」が頻発する。
5〜7個に束ねた時点で、欠損とゆらぎの大半が消える。

行程単位の詳細が必要になった場合は、グループの予測値に固定比率を掛けて割り戻す。
行程ごとに独立したカーブは学習しない。

---

## 4. データ構造

### 4-1. ファイル構成(テスト段階)

手で保守するものはExcel、機械が出力するものはCSVに分ける。
更新頻度と担い手が違うため。

**マスタ:1つのExcelファイル**

| シート | 列 |
|---|---|
| `projects` | 案件ID / 名称 / **種別** / 契約人月 / 開始 / 終了 / タグ / ステータス |
| ~~`milestones`~~ | 案件ID / マイルストーン名 / 日付。**第1版では `projects` シートの右側にマイルストーン列として統合した**(手入力ではシート間の往復と表記の揺れが起きるため)。旧形式も読める |
| `phase_map` | 実績行程名 / 標準行程名 / **行程グループ** / 大分類 |
| `settings` | 換算係数(既定160) / 集約軸 / 位置合わせ / 背骨最小カバー率 / k / タグ重み 等 |

`ステータス` は実装時に追加した列。`予測対象` の案件が予測の対象になり、実績CSVに行が無くてよい。

**実績:CSV**

案件ID / 行程 / メンバー / 所属 / チーム / 月 / 時間

### 4-2. 設計上の指定

**`phase_map` は列を増やせる形にする**
1対1のマッピングではなく、集約軸を複数持てる表とする。
コードを変えずに「行程グループで予測」「大分類で予測」を切り替えられる。
検証時に、どの粒度で学習すると最も当たるかを比較できる。

**読み込み層を1モジュールに閉じ込める**
「DataFrameを複数返す関数」という形にする。
最終的にはデータベース化する想定のため、そこだけ差し替えれば移行できる状態を保つ。

**計算部と出力部を分ける**
予測結果をDataFrameで返す関数を用意しておけば、後からHTML出力を足せる。

**分析対象の定義はマスタが唯一の正とする**
実績CSVは機械が出力する前提のため、別部門の作業・間接業務・廃止された行程・
検証用の案件コードなど、このシステムの対象外のデータが必ず混ざる。
`phase_map` と `projects` に載っているものだけを対象とし、
それ以外は集計する前に落とす。詳細は 5章 Step1・Step2 と 12-2 を参照。

### 4-3. 人月⇔時間の換算

内部の計算はすべて時間ベースで行う。学習するカーブは比率(0〜1)なので単位に依存しない。
換算が必要なのは2か所のみ。

1. 入口:契約人月 × 係数 → 総時間
2. 出口:予測した月次時間 → 人月表示

係数は `settings` シートに持ち、既定160。当面は固定でよいが、変更可能とする。

**出力には換算係数を必ず明記する。**
人月の定義は組織によって揺れるため、数字だけが独り歩きすると議論が噛み合わなくなる。

### 4-4. データ量

実績は数十万行を想定。問題なく処理できる。

理由:学習に必要なのは「案件 × 行程グループ × 月」の合計値であり、
最初の集約で数千行に落ちる。以降の計算は小さい表の上で行われるため、元の行数は効かない。

対策として、読み込み時の `dtype=category` 指定、集約済み中間結果の parquet キャッシュ。

---

## 5. 学習ロジック

すべて行程グループ単位で行う。

### Step 1. 実績の集約
実績を `案件 × 行程グループ × 月` に集約する。

集約の前に、対象外のデータを落とす。

| 落とすもの | 理由 |
|---|---|
| `phase_map` に無い行程 | 対象外業務の工数が行程グループ比率と学習カーブに混ざり、予測が対象外業務の形に引っ張られる |
| `projects` に無い案件ID | 廃番・検証用・別部門のコードが混ざる前提。学習に使える案件かどうかはマスタが決める |

「未分類」グループにまとめて残す案も検討したが却下した。残せば必ず学習に効いてしまう。
ただし**黙って捨てると総量が合わない原因を追えなくなる**ため、
落とした行数・人月・行程名を必ず警告として `条件` シートに残す。

### Step 2. 累積消化カーブの作成
案件ごとに、経過期間比 t(0〜1)→ 累積消化率 F(t) の曲線を作る。

必要なのは月次実績と開始・終了月だけ。
**マイルストーン情報がゼロの案件も必ず学習に貢献できる。** これが学習の土台となる。

**契約期間の外に記録された実績は集計しない。**
遅延・検収対応・保守の続きで実績が終了月をはみ出すのは普通に起きる。
これを端月(開始月・終了月)に寄せて集計すると、はみ出し分がすべて最終月1つに積み上がり、
**元データには存在しない工数の山を最後に作る。** 学習カーブの末尾が跳ね、予測にも転写される。
除外した量は案件別に警告へ残す。割合が大きい案件は、期間指定が実態とずれている可能性が高い。

### Step 3. マイルストーンによる位置合わせ

カーブを平均する**前に**、マイルストーンの位置を揃える。

**意図(この設計の核心)**
α版直前に工数が跳ねる案件が3つあり、α版の位置が t=0.40 / 0.55 / 0.70 とバラついていたとする。
経過期間比のまま平均すると、山は 0.40〜0.70 に塗り広げられ、
**捉えたかった山が平均する行為そのものによって消える。** 案件数が増えるほど徹底的に消える。

先に位置を揃えてから重ねれば、山はそのまま残る。
統計でいう landmark registration にあたる。

**運用**
- 案件間で共通するマイルストーンだけを使って揃える(2点でも単純平均より大幅に改善する)
- α版・β版など、多くの案件に存在するものを「背骨」とする
- マイルストーンは行程の切れ目と一致しなくてよい。**行程との紐づけは不要**、単なる観測点として扱う

**背骨の選定基準は「全案件に存在する」ではなく「カバー率の閾値」とする**(既定 0.6)。
100%必須にすると、1案件で記録が漏れただけで背骨が消え、
他の案件で稼げたはずの精度まで失う。手入力データでは
「存在しなかった」と「記録されていない」の区別がつかない(11章)ため、この全か無かは頻発する。

閾値方式なら、背骨を持たない案件はその点を使わないだけで、残りのアンカーで揃う。
マイルストーンが1つも無い案件は恒等写像となり、素朴版と同じ扱いで平均に参加する。
3-2 の「情報が増えるほど滑らかに精度が上がる」を、この一点でも守る。

これにより、過去マイルストーンを手入力する価値は「表示用」ではなく
**カーブの解像度そのもの**になる。入力の手間を正当化する根拠。

### Step 4. 行程グループ別の工数比率
各グループが総工数の何%を占めるかを学習する。

### Step 5. マイルストーン位置分布
各マイルストーンが経過期間比のどこに来るかを学習する(例:α版は t=0.52 ± 0.07)。
未来プロジェクトのマイルストーン日付を予測するために使う。

### Step 6. 案件の重み付け

**種別による重み付け(第一分類)**

種別が違えばカーブの形も工数比率も異なるため、タグの一致数に埋もれさせない。
ただし完全分離はサンプル不足を招く(10案件を2種別に割ると各5件)。

```
使用する形状 = w × (種別内の平均) + (1 - w) × (全案件の平均)
w = n / (n + k)      n = 種別内の件数、k ≒ 3
```

- 種別内5件 → w=0.63(種別の特徴が主、全体で補強)
- 種別内2件 → w=0.40(全体平均に引き戻して暴れを防ぐ)
- 種別内15件 → w=0.83(ほぼ種別専用モデル)

**意図** ─ データが貯まるほど自動的に「明確に分けた」状態へ近づく。
最初から分離しきる設計だと、少数時は不安定、多数時は最適という切り替えを手動で行うことになる。
この式なら移行が不要。k はパラメータとし、leave-one-out で最適値を検証する。

要素ごとに分け方を変えてもよい(カーブと工数比率は種別差が大きい、所属比率は共通でも耐える)。

**タグによる重み付け**

**絞り込みではなく重み付けとする。**
10案件しかない状態でタグ絞り込みをかけると母数が2〜3件になり、
位置合わせで稼いだ精度をサンプル不足で失う。

全案件を常に使い、タグの一致数と規模・期間の近さから連続値の類似度スコアを出す。
似た案件は重み0.9、遠い案件も0.1は残す。

タグは4〜6個に絞る。10種類も付けると全案件が「唯一無二」になり類似度が機能しない。
規模・期間は数値なので、タグではなく連続的な近さとして別に扱う。

**出力に根拠を残す** ─ 「この予測は PJ-2024-A(重み0.9)、PJ-2023-B(重み0.7) に強く依拠」
と表示し、判断を追えるようにする。

---

## 6. 予測ロジック

### Step 1. 総時間の決定
契約人月 × 換算係数 → 総時間。

見積もり時点で分類ごとの工数が出ている場合は、
**その値を正としてグループ別の総量を固定する**(見積もり分類 = 行程グループのため変換不要)。
決まっていない場合は、学習した工数比率で配分する。

制約の階層:
- 全体の人月のみ(構想段階)→ 全グループを学習値で配分
- 分類ごとの人月あり(見積もり後)→ グループ間は与えられた値を使用

### Step 2. マイルストーン日付の予測

学習した位置分布(Step 5)とタグ・種別の重みから、各マイルストーンの日付を推定する。

**この結果は人が上書きできるようにする。**
「今回はα版を早める方針」といった予定があれば、その1点だけ差し替えれば以降が追従する。
マイルストーンが決まっている場合・いない場合が、同じ仕組みの入力有無だけで切り替わる。

### Step 3. カーブの貼り付け
Step 2 の日付をアンカーとして、位置合わせ済みのカーブを逆変換し、実際の日付軸に貼る。
これによりα版直前の山が、予測されたα版日付の直前に現れる。

動作モード(いずれも同じ仕組み):

| マイルストーン入力 | 挙動 |
|---|---|
| なし | 学習カーブをそのまま期間に貼る(既定) |
| 一部あり | その点だけ実測値に固定、前後は学習カーブで内挿 |
| 多数あり | ほぼ指定どおりの進行を再現 |

案Aのため、どのモードでも**消化率そのものは動かさず、時間軸だけが伸縮する。**

### Step 4. 月次への離散化
暦月で切り出し、合計が総時間に一致するよう正規化して出力する。

### Step 5. 段階予測(途中まで実績が確定している場合)

Step 1〜4 は「着手前に全期間を予測する」形をしているが、
実運用で予測が使われるのは案件が途中まで進んだ時点であり、
問われるのは常に**「ここまでは実績で確定した。では残りはどうなるか」**である。

仕組みは Step 1〜4 の上に載る。追加するのは切り方だけ。

1. 区切り(マイルストーン)を決め、その日付が属する月までを**確定区間**とする
2. 確定区間は実績をそのまま置く
3. 残り区間は、Step 4 で得た月次カーブをその区間だけに絞って正規化し直し、
   **残工数**を配分する

残工数の決め方は2通りあり、どちらが当たるかはデータが決める。

| 決め方 | 総量 | 性質 |
|---|---|---|
| 契約総量固定 | 契約人月(または見積もり)のまま。残り = 総量 − 確定分 | 運用に近い。総量の読み違いがすべて終盤に溜まる |
| 実績スケール | 確定分 ÷ 学習カーブの確定分比率 | 契約人月が実態とずれている案件で効く。序盤は不安定 |

**マイルストーン日付は通過済みのものだけを使う。**
先のマイルストーンの実日付を渡すと未来を覗いた予測になり、評価にならない。
Step 2 の「人による上書き」を、名前の集合で絞れるようにしただけで実現している
(`forecast(known_milestones=...)`)。

**評価対象は残り区間だけ。** 確定分は実績そのもので誤差0のため、
混ぜると段階が進むほど自動的に数字が良くなり、
予測が良くなったのか確定が増えただけなのか区別できなくなる。

**学習からは対象案件を必ず外す。** 完了案件を対象にして予測の妥当性を測るのが
この仕組みの主用途であり、自分の実績で学習したカーブで自分を予測しては評価にならない。

実装: `src/phased.py` / 出力は `段階予測` シート / 全案件での評価は
`scripts/evaluate_phased.py`。

---

## 7. 出力

**Excel形式。**

**意図** ─ テスト段階でやりたいのは「切り替えて見る」だけでなく
**数字を疑って確かめる**こと。その場でフィルタをかけ、実績と並べ、手で検算できる。
HTMLでは値を疑ったときコードに戻るしかない。
またグラフの切り替え(表示/非表示、積み上げ/折れ線)はExcel側の機能で賄えるため、
**作らずに済む機能が多い。** 関係者にそのまま渡せる利点もある。

| シート | 内容 |
|---|---|
| `予測` | **月 × 行程グループ の表(主役)** + 積み上げグラフ |
| `段階予測` | マイルストーンごとに実績を確定させた予測を、実績に重ねた折れ線(月次・累積) |
| `カーブ` | 学習した平均カーブと、各案件のカーブの重ね描き |
| `検証` | leave-one-out の結果。案件別・月別の予測 vs 実績、誤差 |
| `条件` | 使用パラメータ、重み付けされた案件と重み、換算係数 |

**`カーブ` シートの意図** ─ 10本の実績カーブに平均カーブを重ねると、
形が違う外れ値の案件が一目で分かる。「種別分けが間違っているのでは」という気づきは
表よりグラフのほうが早い。

**`段階予測` シートの意図** ─ 予測が当たっているかを、案件の完了を待たずに測る。
1本の予測が当たったかどうかは運でも決まるが、
**確定した実績が増えるほど予測が実績へ寄っていくか**は仕組みの性質であり、
寄っていかないなら、その予測は追加された情報を使えていない。
グラフに実績を太い線で置き、各段階の予測を重ねるのは、その収束を目で見るため。

**`条件` シートの意図** ─ パラメータを何度も変える段階では、
「この結果はどの設定で出したか」を追えないと結果の比較ができなくなる。

**パラメータ比較** ─ 出力ファイル名に条件を埋め込む(`forecast_k3_align-on.xlsx`)か、
複数条件を一度に計算して `比較` シートに横並びにするモードを持たせると効率が上がる。

---

## 8. 検証方法

**leave-one-out** ─ 1件を除いた9件で学習し、除いた1件を予測して実績と比較する。
10件しかないため、これが唯一まともな評価手段。

**最初から組み込むこと。**
位置合わせ、種別の重み付け、タグの重み付けが本当に効いているかを数字で確認できる。
効いていないパラメータを削れるため、結果的に実装が小さくなる。

**段階予測による検証**(6章 Step5)─ leave-one-out が測るのは
「着手前の予測が当たるか」だけで、実運用で使われる
「途中まで確定した状態からの予測」の当たり外れは測れない。
完了案件をマイルストーンで区切り、段階ごとの残り区間の誤差を並べる。
確定が増えるほど誤差が下がるなら、予測は追加された実績を使えている。
`scripts/evaluate_phased.py` が全完了案件でこれを回す。

---

## 9. 実装順序

**素朴版から始め、機能を1つずつ足して効果を測る。**

| # | 内容 | 状態 |
|---|---|---|
| 1 | **素朴版** ─ 全案件のカーブを単純平均し、期間に貼るだけ。leave-one-out で誤差を測定 | **実装済** |
| 2 | **+ マイルストーン位置合わせ** ─ 誤差の変化を確認 | **実装済**(12-1 に結果) |
| 3 | **+ 種別の重み付け** ─ k = 0 / 3 / ∞ を比較 | 未着手 |
| 4 | **+ タグの重み付け** ─ 種別導入後も効果があるか確認 | 未着手 |
| 5 | **+ 見積もり分類による総量固定** | 未着手(`forecast(group_totals=...)` に口だけ用意) |

3・4 は `learn(weights=...)` に案件IDごとの重み辞書を渡す形になっているため、
重みを計算する関数を1つ足せば進める。`validation.py` の `MODES` にモードを足せば
そのまま leave-one-out で効果を測れる。

**意図** ─ 10案件では効かない機能が混じる可能性が十分にある。
特にタグの重み付けは、種別を入れた後では効果が出ないかもしれない。
最初から全部入りで作ると、どれが効いているか分からないまま複雑なコードだけが残る。

素朴版は小さく作れるため、データが揃った時点ですぐ動かせる。
そこで得られる誤差の数字が、以降すべての判断基準になる。

---

## 10. 決定事項一覧

| 項目 | 決定 | 理由 |
|---|---|---|
| 期間と工数の関係 | **案A**(比率固定・期間のみ伸縮) | 期間延長は承認待ち等が要因で、作業量増ではない |
| 弾力性パラメータ α | **不採用** | 案Aで確定したため不要 |
| 過去マイルストーンの取得 | **手入力のみ** | 自動判別は行わない |
| マイルストーンと行程の紐づけ | **不要** | α版等は行程の切れ目と一致しない。観測点として扱う |
| 契約人月と実績の乖離率 | **予測には使わない**が診断用に表示する | 予測には不要。ただし乖離が大きい案件は実績の記録漏れを疑うべきなので、`条件` シートに並べる |
| 人月換算 | **固定値160**(変更可) | 当面固定でよい。出力に必ず明記 |
| 学習の粒度 | **行程グループ** | 細かい行程は観測点不足・欠損多発 |
| 見積もり分類 | **行程グループと一致** | 変換不要。見積もり値をそのまま制約にできる |
| 種別 | **第一分類**(必須・排他、2〜3個) | タグに埋もれさせない。5個作ると全部2件になる |
| 種別の扱い | **w = n/(n+k) で全体と混合** | 完全分離はサンプル不足を招く。データ増で自動移行 |
| タグ | **絞り込みではなく重み付け**(4〜6個) | 絞り込むと母数が枯れる |
| 所属・チーム・メンバー | **補助情報**。集計軸は行程 | 主軸が属性欠損で壊れないようにする |
| 出力形式 | **Excel** | 検算・探索に強い。グラフ機能を実装せずに済む |
| HTML / Web UI | **後回し** | 探索フェーズではなく共有フェーズの課題 |
| 背骨マイルストーンの選定 | **カバー率の閾値**(既定0.6) | 全案件必須にすると1件の記録漏れで機能ごと失う |
| `phase_map` に無い行程 | **集計前に除外**(量は警告に残す) | 対象外業務の形に予測が引っ張られる |
| `projects` に無い案件ID | **集計前に除外**(量は警告に残す) | 学習対象かどうかはマスタが決める |
| 契約期間外の実績 | **集計しない**(端月に寄せない) | 寄せると元データに無い工数の山を最終月に作る |
| 過去案件の契約人月が空欄 | **実績合計で補完**して学習を続ける | 形の学習には契約人月を使わない。除外すると実績が丸ごと無駄になる |
| 予測対象の契約人月が空欄 | **エラーで停止** | 総工数を決められない。空の成果物を黙って出すのが最悪 |

---

## 11. 未決事項(実データを見てから決める)

| 項目 | 内容 | 状態 |
|---|---|---|
| 行程グループの切り方 | 20〜30の行程をどう5〜7個に束ねるか。見積もり分類に合わせる | サンプルでは7個で仮置き。実データの見積もり分類に合わせて要調整 |
| 行程名の表記統一 | 粒度・表記が揃っていないはず。`phase_map` で吸収 | **要注意**。`phase_map` に無い名前は除外されるため、揺れがそのまま欠損になる(12-2) |
| マイルストーン名の背骨 | 実質的に存在するものを選ぶ | カバー率0.6で自動選定。`settings` で明示指定も可 |
| マイルストーン名の正規化 | 「α版」「アルファ版」の揺れ | 未対応。揺れが出たら `ms_master` シートを追加する |
| 種別の具体的な粒度 | 2つで足りるか | サンプルでは3個(新規タイトル開発 / 大型アップデート / 移植・リマスター) |
| タグの候補 | 4〜6個 | サンプルでは5個。第1版では未使用 |
| k の最適値 | leave-one-out で決定 | 実装順序3で検証する |

**手入力データの注意点** ─ マイルストーンが手入力のため、「入力が無い」は
「存在しなかった」と「記録されていない」の区別がつかない。
また、α版を設けるのは大規模案件に偏る等の傾向も出やすい。
対策として、統計の根拠になった案件名を必ず出力に載せ、人が確認できるようにする。
件数も併記し(例:「α版:平均62%(n=2、参考値)」)、閾値未満は警告する。

---

## 12. 実装で判明したこと

第1版(実装順序 1〜2)を作り、サンプルデータで動かして分かったこと。
設計時の想定と違った点を残す。

### 12-1. 位置合わせは、それ単体では効かない

leave-one-out の結果(10案件のサンプルデータ)。

| モード | 月次誤差WAPE | 累積カーブ最大乖離 | 素朴版比 |
|---|---|---|---|
| 素朴版 | 0.262 | 0.095 | — |
| 位置合わせ(対象案件のMSは学習した平均位置を使用) | 0.273 | 0.103 | **−4.4%** |
| 位置合わせ(対象案件のMS日付を既知として指定) | 0.079 | 0.032 | **+69.7%** |

**平均カーブを鋭くしただけでは改善しない。むしろ悪化する。**
鋭い山を平均位置に置くと、実際の山とズレたときの誤差はかえって大きくなるため。
効いたのは、対象案件のマイルストーン日付を与えたときだけだった。

5章 Step3 の「手入力の価値は表示用ではなくカーブの解像度そのもの」という主張は
裏付けられた。同時に、**未来案件のマイルストーンが未定なら素朴版と大差ない**という
制約も明らかになった。位置合わせの価値は「過去分の入力」ではなく
「過去分と未来分の両方の入力」に対して発生する。

マイルストーンの整備状況に対する縮退は滑らかである。

| 整備状況 | 背骨 | 素朴版比の改善 |
|---|---|---|
| 全10案件にα版・β版・マスターアップあり | 3点 | +69.7% |
| 2案件で記録が欠損 | 3点(欠損案件は使わないだけ) | +51.7% |
| 全案件で記録なし | なし | ±0%(素朴版と同一) |

**注意** ─ サンプルデータは正準時間軸上で定義した密度を、案件ごとにばらつかせた
マイルストーン位置へワープさせて生成している。つまり**位置合わせが効く構造を
最初から持たせてある**。上の数字は実装が設計どおり動く確認であって、
実データで同じ改善が出る保証ではない。実データでは測り直す必要がある。

### 12-2. 予測が壊れる原因は、ほぼすべてデータの前処理側にある

実装中に遭遇した不具合は、予測ロジックではなくデータの取り込み方に起因していた。
症状から原因を引くための対応表を残す。

| 症状 | 原因 | 対処 |
|---|---|---|
| 期間の最後5%で工数が急上昇する | 契約期間外の実績を最終月に寄せて集計していた | 5章 Step2。期間外は集計しない |
| 特定の行程グループの比率が不自然に高い | 対象外業務を「未分類」として残していた | 5章 Step1。`phase_map` 外は除外 |
| 消化率が t=0.5〜0.7 で100%に達する | 後半の工数が集計から欠落している。原因は複数あり得る(下記) | 個別に切り分け |

**消化率が途中で100%に達する場合の切り分け** ─ 再現実験で確認した原因候補。

| 原因 | 再現時の到達点 | 見分け方 |
|---|---|---|
| `終了` が実態より後ろに設定されている | — | 実績が実在する最終月と `終了` を比較 |
| 後半に偏る行程だけ命名規則が違う(`デバッグ(協力会社)` 等) | t=0.70 | 除外警告の行程名に、`phase_map` と似た名前が無いか |
| 途中から月の書式が変わっている(`2022-07` と `2022/7`) | t=0.50 | 期間外として除外された行の `月` の値を見る |
| 後半の工数が別案件IDに付け替えられている(枝番・保守コード) | t=0.58 | 未登録IDの警告に、対象案件と似たIDが無いか |
| 中止・凍結された案件 | — | データの誤りではない。学習から外すべき案件 |

いずれも「後半の工数が集計から消える」型である。
**元データを目視しても山や欠落は見えない**点が共通しており、
落とした量を警告として残す設計(5章 Step1・Step2)が切り分けの前提になる。

### 12-3. 実績を確定させても、残り区間の予測はほとんど当たるようにならない

段階予測(6章 Step5)を完了10案件で回した結果。残り区間の月次誤差WAPE の平均。

| 確定した期間 | 契約総量固定 | 実績スケール | 残りが全体に占める割合 |
|---|---|---|---|
| 0%(着手前) | 0.357 | 0.357 | 100% |
| 〜25% | **0.262** | 0.716 | 83% |
| 〜50% | **0.315** | 0.880 | 72% |
| 〜75% | **0.283** | 0.678 | 46% |
| 75%超 | **0.383** | 1.087 | 8% |

**確定した実績を増やしても、残り区間の精度は大きくは上がらない(0.357 → 0.26〜0.38)。**
現在のモデルは学習した平均カーブを期間に貼るだけで、
**確定区間に現れた形をカーブの選択に反映していない**ため、これは当然の帰結である。
確定分が効いているのは総量の按分だけになっている。

これは 12-1 と同じ構造の発見にあたる。位置合わせは「山を鋭くする」仕組みだが
貼る位置の情報が無ければ効かなかった。段階予測は「情報を足す口」を作ったが、
その情報を形の選択に使う仕組みがまだ無い。
**入れ物を作ることと、情報が効くことは別**である。

改善するとしたら次の2つで、どちらも段階予測の枠組みの上で効果を測れる。

1. **確定区間の形が似ている過去案件に重みを寄せる**(9章 実装順序3 の重み付けの応用)。
   `learn(weights=...)` に重み辞書を渡す口は既に開いている。
   「この案件は後半集中型だ」と途中で判定できれば、残りの形が変わる。
2. **確定区間から総量の読み違いを検知する。** `実績スケール` はその最も素朴な形で、
   序盤に不安定なのは1点のみの外挿だからである。確定分の長さを信頼度として
   `契約総量固定` と加重平均するだけでも改善の余地がある。

`実績スケール` はどの区間でも `契約総量固定` に劣る。確定分1点からの外挿であり、
そこに乗った案件固有のばらつきをそのまま総量に増幅するためで、
契約人月という外から与えられた情報のほうが現状では強い。
既定は `契約総量固定` のままとし、`実績スケール` は
契約人月が実態とずれていると分かっている案件で試す位置づけにとどめる。

なお 12-1 と同じ注意が付く。サンプルデータは学習カーブと同じ発想で生成しているため、
この数字は実データでの結論ではない。実データを入れたら測り直すこと。

### 12-4. 配分の「合計が保存されているか」は、実行時に検算する

段階予測の実装で、案件の総工数が段階ごとに増えていく不具合を出した。
グラフ上は**マイルストーンの直後に工数が跳ねる**形で現れ、
合計を取ってみるまで原因が分からなかった。

原因は、行程グループごとに 残り = 総量 − 確定分 を取り、
確定分が総量を超えたグループの負値を `max(..., 0)` で切っていたこと。
切られた超過分がどこにも計上されず、その分だけ案件の総工数が増えていた。
実測では契約 472,000 時間に対し、最終段階で 498,650 時間(+5.6%)。

正しくは、案件全体の残工数 Σ総量 − Σ確定分 を正とし、
まだ残っているグループへその比率で配り直す。超過は
「他のグループの残りを前借りした」と読む。

得られた教訓は2つある。

- **クリップは、切った量の行き先を必ず決める。** 負値を 0 にするのは
  局所的には妥当に見えるが、合計を保存する仕組みが別に無ければ総量が動く。
  この種のバグは値そのものではなく合計を取らないと見えない。
- **保存則は表明として実装に埋める。** 段階の総工数が見込み総量と一致することを
  `phased_forecast()` の中で毎回検算し、崩れたら止めるようにした。
  出力の数字を人が疑うまで見つからない不具合は、コード側で先に見つける。

同じ性質の不具合は 12-2 の「後半の工数が集計から消える」型と同じで、
**元データを目視しても見えず、合計を取って初めて分かる**。

### 12-5. 出力の数字は検算できる形で出す

- 合計はすべて SUM 数式で書く。値を疑ったとき、その場で検算できる
- 人月表示は「時間 ÷ 換算係数」の数式にし、係数セルを参照させる。
  係数を書き換えれば表全体が追従し、数字だけが独り歩きしない(4-3)
- 除外・補完・縮退は、標準出力ではなく `条件` シートに残す。
  Excelを受け取った人には標準出力が見えないため

---

## 13. 将来の拡張

初期スコープ外としたもの。データ構造は最初から対応しておき、出力のみ後回しとする。

- **所属・チーム別の内訳** ─ 各セルに構成比を掛けて算出
- **頭数と稼働率** ─ メンバー名から実人数を数え、時間÷160の要員換算とのズレを学習する。
  「換算7.5人 = 実際には10〜12人が必要」という調達視点の数字が出せる。
  ただし総量制約とは無関係な参考値であることを明示する
- **ピーク要員の警告** ─ 案Aの帰結として後半に工数が集中するため、調達可能性の検証が有用
- **行程単位の内訳** ─ グループ予測値に固定比率を掛けて割り戻す
- **P50 / P80 の幅** ─ 過去のばらつきから楽観〜悲観の範囲を出す
- **進行中プロジェクトの早期警戒** ─ 学習した消化率カーブは監視にそのまま使える。
  「基本設計完了時点で45%(基準30%)」なら赤信号。予測ツールが監視ツールを兼ねる
- **HTML / Streamlit UI** ─ 共有フェーズ向け
- **データベース化** ─ 読み込み層の差し替えで対応
